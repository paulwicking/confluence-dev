language: python
sudo: true 
matrix:
  fast_finish: false
os:
- linux
addons:
  hostname: python-confluence-test
python:
- '2.7'
- '3.4'
- '3.5'
- '3.6'
before_install:
- echo "deb https://sdkrepo.atlassian.com/debian/ stable contrib" | sudo tee -a /etc/apt/sources.list
- sudo apt-key adv --keyserver hkp://keyserver.ubuntu.com:80 --recv-keys B07804338C015B73
- sudo apt-get update -qq
install:
- sudo apt-get install apt-transport-https
- sudo apt-get install atlassian-plugin-sdk
- pip -q --log dist/pip.log install --upgrade pip setuptools tox-travis py wheel
- python setup.py sdist bdist_wheel install
- pip install ./dist/*.whl
- pip --version
before_script:
- atlas-run-standalone -q -B -DskipAllPrompts=true --product confluence --version 5.9.14 </dev/zero 2>&1 &
- sleep 300
script:
- export PACKAGE_NAME=$(python setup.py --name)
- export PACKAGE_VERSION=$(python setup.py --version)
- python setup.py --version
- tox --installpkg ./dist/*.whl --travis-after
# validates that the build source distribution is installable using the old easy_install
- pip uninstall -y $PACKAGE_NAME && easy_install ./dist/$PACKAGE_NAME-*.tar.gz
after_success:
#- coveralls
#- bash <(curl -s https://codecov.io/bash)
#- requires.io update-site -t ac3bbcca32ae03237a6aae2b02eb9411045489bb -r
notifications:
  email:
  - paulwicking@gmail.com
